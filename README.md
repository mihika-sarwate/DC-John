# MENTORIA Frontend

Next.js frontend for MENTORIA career guidance platform.

## Setup

1. Install dependencies: `npm install`
2. Build static site: `npm run build`
3. Output in `out/` directory

## Environment

All content fetched from Sanity project `6w0t9e7l`
